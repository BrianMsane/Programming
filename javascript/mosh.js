// separation of concerns - separate HTML from JavaScript and CSS

// variables(var is being depracated)
let myName = "Thandokuhle";
const surname = "Msane";
let middleName = "Brian";
console.log(`Your name is ${name}`);

// constants(we cannot change then once declared like c++, c, or static in java)
const fullNmae = myName + surname + middleName;
